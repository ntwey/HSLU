```Java
public class DatentypDemo {
    public void demo() {
        // Ganzzahlen: byte, short, int, long [cite: 36]
        long grosseZahl = 3_000_000_000L; 

        // Gleitkomma: float (32-bit), double (64-bit) [cite: 36]
        // Wichtig: double hat eine höhere Genauigkeit als float [cite: 37]
        double d = 0.1 + 0.2; // Achtung: Ungenauigkeit bei binärer Darstellung!
        
        // Wahrheitswerte & Zeichen [cite: 36]
        boolean istWahr = true;
        char zeichen = 'A';
    }
}
```
